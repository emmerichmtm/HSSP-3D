[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "hssp3d"
version = "0.1.0"
description = "3-D hypervolume subset selection in n^O(sqrt k) time (planar-separator dynamic programme), Python + Numba"
readme = "README.md"
authors = [{ name = "Michael Emmerich" }]
license = { text = "MIT" }
requires-python = ">=3.9"
dependencies = ["numpy", "numba>=0.57"]

[project.optional-dependencies]
test = ["pytest"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
